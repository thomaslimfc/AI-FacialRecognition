Check for installed pip and Python versions
    py --version
  then
    py -m pip --version
    OR
    pip --version

Install necessary libs with pip
    pip install ultralytics opencv-python


# Deepface Algotithm
    pip install deepface
